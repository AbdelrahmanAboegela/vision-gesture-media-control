__pycache__/
*.py[cod]

.venv/
venv/
env/

config/*.broken
config/custom_gestures.json

# Runtime data and downloaded model assets can be large or user-specific.
data/db/*
models/*.onnx
models/*.task
models/*.zip
models/vosk-*/

# Keep the directories documented even when runtime files are removed.
!config/custom_gestures.example.json
!models/.gitkeep
!data/.gitkeep
!data/db/.gitkeep
